{
  "name": "nexora-platform",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install:all": "npm ci --prefix apps/nexora && npm ci --prefix apps/nexora-mcp",
    "check": "npm run check --prefix apps/nexora && npm run check --prefix apps/nexora-mcp",
    "test": "npm test --prefix apps/nexora && npm test --prefix apps/nexora-mcp && node apps/nexora-mcp/tests/protocol.mjs && node scripts/verify-repository.mjs",
    "verify": "npm run check && npm test",
    "smoke:production": "node scripts/smoke-production.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
