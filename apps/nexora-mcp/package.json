{
  "name": "nexora-chatgpt-mcp",
  "version": "1.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check netlify/functions/gateway.mts && node --check src/nexora.mts && node --check src/tools.mts",
    "test": "node --test tests/domain.test.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.25.0",
    "firebase-admin": "^13.0.0",
    "zod": "^3.25.0"
  },
  "engines": {
    "node": ">=22"
  }
}
